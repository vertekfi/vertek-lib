export class FeeManagement {
  async checkpointFeeDistribution() {}

  async getPendingFeeTokens() {
    // subgraph all tokens and get distributors balances
    // V2 is BPT, so really just all pools
  }
}
